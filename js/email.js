// Sends uploaded image + order info to your email
function sendOrderEmail(orderNumber, customerName, file) {
    console.log("Preparing email...", orderNumber, customerName, file);

    // Placeholder — we will connect this to your backend or Square webhook
    alert("Email would be sent here.");
}
